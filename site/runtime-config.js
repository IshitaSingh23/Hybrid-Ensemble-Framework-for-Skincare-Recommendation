// Runtime deployment config for static hosts such as Render.
// Replace apiUrl after the FastAPI backend URL exists, for example:
// window.__UPSKIN_RUNTIME_CONFIG = { apiUrl: "https://upskin-api.onrender.com" };
window.__UPSKIN_RUNTIME_CONFIG = window.__UPSKIN_RUNTIME_CONFIG || {
  apiUrl: "",
};
